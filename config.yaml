# Paw 核心配置文件

# API 配置
api:
  # API密钥
  key: "3dae586b23fe4f19ab52a0152eebe182.luZt04U5Xbeu2sPl"
  
  # API 端点 URL
  url: "https://open.bigmodel.cn/api/coding/paas/v4/chat/completions"
  
  # 模型名称 - 留空 (null) 则在启动时选择
  #model: GLM-4.6

  #最近太穷了，只能先用本地模型做测试了
  #url: "http://localhost:1234/v1/chat/completions"
  model: null

# 终端配置
terminal:
  # Shell 类型选择:
  # Windows: 'powershell', 'cmd'
  # Unix/Linux: 'bash', 'zsh'
  # 默认: 'powershell' (Windows) 或 'bash' (Unix)
  shell: 'powershell'
  
  # 字符编码
  encoding: 'utf-8'

# 系统配置
system:
  # 上下文窗口大小（token）
  chunk_size: 64000
