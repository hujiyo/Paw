# Paw 核心配置文件

# 身份配置
identity:
  # 用户给 Paw 起的昵称
  name: "Paw"
  # 用户名
  username: "hujiyo"
  # 用户昵称
  honey: "老公"

# API 配置
api:
  # API密钥
  key: "aef418d2241a4e008fcdd61370cef5cc.VDXiXUnwJLWHcliy"
  #key: "sk-W0rpStc95T7JVYVwDYc29IyirjtpPPby6SozFMQr17m8KWeo"
  
  # API 端点 URL
  url: "https://open.bigmodel.cn/api/coding/paas/v4/chat/completions"
  #url: "https://open.bigmodel.cn/api/paas/v4/chat/completions"
  
  # 模型名称 - 留空 (null) 则在启动时选择
  #url: "http://localhost:1234/v1/chat/completions"
  model: null

# Embeddings 配置（用于记忆系统向量化）
embeddings:
  # 可选：显式指定本地 embedding 模型路径（.gguf）
  # 留空则自动从项目根目录 embedding/*.gguf 选择第一个
  path: ""

# 终端配置
terminal:
  # Shell 类型选择:
  # Windows: 'powershell', 'cmd'
  # Unix/Linux: 'bash', 'zsh'
  # 默认: 'powershell' (Windows) 或 'bash' (Unix)
  shell: 'powershell'
  
  # 字符编码
  encoding: 'utf-8'

# Web 工具配置
web:
  # 搜索引擎: duckduckgo (免费，无需 API Key)
  # 未来可扩展: google, bing (需要 API Key)
  search_engine: 'duckduckgo'
  
  # 搜索结果数量（默认5条）
  max_results: 5
  
  # 每页最大字节数 (4KB)
  page_size: 4096
  
  # 使用 Jina Reader 代理获取网页（支持 JS 动态渲染）
  # true: 通过 r.jina.ai 获取（推荐，免费，支持 SPA）
  # false: 直接获取（仅支持静态页面）
  use_jina_reader: true

#  custom_search_api:
#    name: "baidu"
#    url: "https://api.baidu.com/search?q={query_encoded}&apikey={api_key}"
#    method: "GET"
#    api_key: "你的百度API密钥"
#    results_path: "result.items"     # 视百度返回JSON结构而定
#    title_field: "title"
#    url_field: "url"
#    snippet_field: "summary"

# 系统配置
system:
  # 上下文窗口大小（token）
  chunk_size: 64000
