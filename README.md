# Paw - 数字生命体

Paw 是一个完全符合 OpenAI Function Calling 标准的 AI Agent，采用流式输出和简约界面设计。

**设计哲学**：
- 对话历史只存在于内存中，每次启动都是全新开始
- 记忆完全由 Paw 自己决定和管理（存储在工作目录）
- 简洁、轻量、无历史包袱

一个完全符合 OpenAI Function Calling 标准的 AI Agent，采用流式输出和简约界面设计。

## 核心特性

- 🔧 **标准化** - 完全符合 OpenAI Function Calling 规范
- 💨 **流式输出** - 实时显示 AI 生成内容
- 📊 **语块系统** - 统一的上下文管理
- 🎨 **简约界面** - 参考 Claude Code 设计理念

## 快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 运行Paw (Windows)
.\scripts\paw.bat

# 或直接运行
python paw.py
```

## OpenAI 标准实现

### Function Calling 标准流程

完全符合 OpenAI 官方规范：

```python
# 1. 用户输入
messages = [
    {"role": "system", "content": "..."},
    {"role": "user", "content": "列出txt文件"}
]

# 2. 模型返回（包含tool_calls）
{
    "role": "assistant",
    "content": None,
    "tool_calls": [{
        "id": "call_xxx",
        "function": {"name": "list_directory", "arguments": "{}"}
    }]
}

# 3. 工具结果（role="tool"）
{
    "role": "tool",
    "tool_call_id": "call_xxx",
    "name": "list_directory",
    "content": "文件列表..."
}

# 4. 最终回复
{
    "role": "assistant",
    "content": "找到5个txt文件"
}
```

### 创新点：语块系统

`chunk_manager` 是**唯一的上下文管理器**，负责：
- 📦 统一管理所有消息（system, user, assistant, tool）
- 🔄 精确转换为OpenAI标准格式（包含tool_calls, tool_call_id等）
- 📊 上下文可视化（`/chunks`命令显示LLM实际视角）
- 🎯 未来支持上下文工程（压缩、优先级、摘要等）

## 简约界面

参考 Claude Code 的设计理念 + **流式输出**：

```
> 列出当前目录的文件

🔨 list_directory({})
  file1.txt, file2.py, README.md

找到3个文件  ← 逐字打印，像打字机一样
```

**颜色系统**：
- **白色（亮）** - 用户输入
- **绿色** - AI 回复
- **蓝色（暗）** - 工具调用
- **灰色** - 工具结果
- **红色** - 错误信息

## 命令

- `/model` - 检测并切换可用模型
- `/chunks` - 查看完整上下文（**LLM实际视角**，包含tool_calls等完整信息）
- `/messages` - 查看原始消息格式（OpenAI标准，调试用）
- `/clear` - 清空历史
- `exit` - 退出

## 项目结构

```
Paw/
├── paw.py              # 主程序入口
├── autostatus.py       # 动态状态评估
├── chunk_system.py     # 语块系统
├── prompts.py          # 提示词配置
├── tools.py            # 基础工具
├── tools_schema.py     # 工具定义
├── tool_errors.py      # 错误处理
├── terminal.py   # 异步终端管理
├── config.yaml         # 配置文件
├── requirements.txt    # 依赖
├── README.md           # 文档
└── scripts/            # 启动脚本
    ├── paw.bat
    └── add_to_path.ps1
```

## 基础工具

- `read_file(path)` - 读取文件
- `write_file(path, content)` - 创建文件
- `list_directory(path)` - 探索目录
- `execute_command(cmd)` - 执行命令
- `run_script(lang, code)` - 运行脚本

## 工作空间

Paw 使用 `桌面/Paw-workspace/` 作为工作空间：
- `logs/` - 会话日志

---

*"The future is not about AI serving humans, but AI and humans coexisting in their respective worlds."*
